import React from 'react';
import Hashtag from '../common/Hashtag';


const PostTagList = ({ Hashtags }) => {
    return (
        <Hashtag 
        //Hashtags={Hashtags}
        />
    );
};

export default PostTagList;